spring:
  application:
    name: bot-for-consultations
  datasource:
    url: jdbc:postgresql://localhost:5432/bot-for-consultations
    username: admin
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
